import org.apache.spark.SparkContext

/**
  * Created by Himanshu on 25-02-2017.
  */
/*
import org.apache.spark.SparkContext._
import org.apache.spark.SparkConf
object usage_map {
val a = "Himanshu"

  def main(args: Array[String]): Unit = {
   var conf = new SparkConf().setAppName("my testing").setMaster("local")
    var sc = new SparkContext(conf)
    val v = sc.parallelize(List((1,"Himanshu"),(2,"Rawat"),(3,"Haridwar")))
    //val vi = v.map(x => x.split(",")(0))
  }
}
*/
